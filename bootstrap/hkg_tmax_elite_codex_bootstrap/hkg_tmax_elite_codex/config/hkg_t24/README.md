# HKG T24 Full Strategy Configuration

Configuration for the HKG T+24 / H24N full strategy implementation belongs here.

Expected future contents include fold specifications, feature schema configuration, station dossier configuration, source policy configuration, and sealed-validation guards required by the Jira packets.

Do not put secrets in this folder. Local credentials remain under `secrets/local/` according to `AGENTS.md`.
