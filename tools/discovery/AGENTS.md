# Discovery tool rules

The root `AGENTS.md` applies. Discovery is read-only by default. Bound directory
scope, page count, requests, bytes, retries, runtime, and concurrency. Do not
follow junctions, scan external datasets, persist provider data, or mutate a
database during orientation. Any live provider discovery requires explicit
execution authorization and a documented budget.
