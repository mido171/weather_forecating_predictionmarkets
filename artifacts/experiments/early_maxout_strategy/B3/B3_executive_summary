B3
Batch B3 Executive Summary

EXP11_BASELINE_REPRO
EXP11_BASELINE_REPRO is a LightGBM classifier experiment that focuses on reproducing the prior convective block baseline. It uses minute plus MOS. The purpose is anchor v2 results against v1 best. On the test split it achieved accuracy 0.736, balanced accuracy 0.684, YES precision 0.727, and YES recall 0.475. On validation it achieved accuracy 0.748 and balanced accuracy 0.691. This result shows moderate skill with useful signal. It serves as a baseline comparator for later variants. These notes emphasize leak-safe comparison across batches and clarify precision/recall tradeoffs for future revisions and ablations.
Clarified interpretation: The input features and model family are exactly as stated above; no additional signals are introduced. The model makes a single binary decision at the cutoff: has the daily maximum (Tmax) already occurred? YES precision reflects avoiding false early-peak calls, while YES recall reflects avoiding missed late-heating cases. Use these metrics strictly for relative ranking within the same split and thresholds, not as a trading PnL claim. If this variant underperforms, it usually means the added hypothesis is noisy, weak, or needs better calibration. Compare it to the closest baseline in this batch and focus on balanced accuracy and the precision/recall tradeoff together.

EXP12_ADD_HEAT_GAP
EXP12_ADD_HEAT_GAP is a LightGBM classifier experiment that focuses on remaining heating gap features. It uses minute plus MOS. The purpose is test direct heating potential signals. On the test split it achieved accuracy 0.746, balanced accuracy 0.701, YES precision 0.724, and YES recall 0.522. On validation it achieved accuracy 0.757 and balanced accuracy 0.715. This result shows moderate skill with useful signal. It serves as a core fusion benchmark. These notes emphasize leak-safe comparison across batches and clarify precision/recall tradeoffs for future revisions and ablations.
Clarified interpretation: The input features and model family are exactly as stated above; no additional signals are introduced. The model makes a single binary decision at the cutoff: has the daily maximum (Tmax) already occurred? YES precision reflects avoiding false early-peak calls, while YES recall reflects avoiding missed late-heating cases. Use these metrics strictly for relative ranking within the same split and thresholds, not as a trading PnL claim. If this variant underperforms, it usually means the added hypothesis is noisy, weak, or needs better calibration. Compare it to the closest baseline in this batch and focus on balanced accuracy and the precision/recall tradeoff together.

EXP13_ADD_MOS_TBLOCK_MISMATCH
EXP13_ADD_MOS_TBLOCK_MISMATCH is a LightGBM classifier experiment that focuses on MOS time block mismatch vs observed temp. It uses minute plus MOS. The purpose is use model observation mismatches as signal. On the test split it achieved accuracy 0.731, balanced accuracy 0.669, YES precision 0.749, and YES recall 0.423. On validation it achieved accuracy 0.746 and balanced accuracy 0.682. This result shows moderate skill with useful signal. It serves as a core fusion benchmark. These notes emphasize leak-safe comparison across batches and clarify precision/recall tradeoffs for future revisions and ablations.
Clarified interpretation: The input features and model family are exactly as stated above; no additional signals are introduced. The model makes a single binary decision at the cutoff: has the daily maximum (Tmax) already occurred? YES precision reflects avoiding false early-peak calls, while YES recall reflects avoiding missed late-heating cases. Use these metrics strictly for relative ranking within the same split and thresholds, not as a trading PnL claim. If this variant underperforms, it usually means the added hypothesis is noisy, weak, or needs better calibration. Compare it to the closest baseline in this batch and focus on balanced accuracy and the precision/recall tradeoff together.

EXP14_ADD_PEAK_CONFIDENCE_V2
EXP14_ADD_PEAK_CONFIDENCE_V2 is a LightGBM classifier experiment that focuses on peak confidence and curve stability features. It uses minute plus MOS. The purpose is detect post peak shapes more explicitly. On the test split it achieved accuracy 0.719, balanced accuracy 0.669, YES precision 0.679, and YES recall 0.472. On validation it achieved accuracy 0.753 and balanced accuracy 0.702. This result shows modest skill and partial signal capture. It serves as a core fusion benchmark. These notes emphasize leak-safe comparison across batches and clarify precision/recall tradeoffs for future revisions and ablations.
Clarified interpretation: The input features and model family are exactly as stated above; no additional signals are introduced. The model makes a single binary decision at the cutoff: has the daily maximum (Tmax) already occurred? YES precision reflects avoiding false early-peak calls, while YES recall reflects avoiding missed late-heating cases. Use these metrics strictly for relative ranking within the same split and thresholds, not as a trading PnL claim. If this variant underperforms, it usually means the added hypothesis is noisy, weak, or needs better calibration. Compare it to the closest baseline in this batch and focus on balanced accuracy and the precision/recall tradeoff together.

EXP15_ADD_CONDITIONAL_CLIMO
EXP15_ADD_CONDITIONAL_CLIMO is a LightGBM classifier experiment that focuses on conditional climatology priors. It uses minute plus MOS. The purpose is inject seasonal priors and deltas. On the test split it achieved accuracy 0.728, balanced accuracy 0.659, YES precision 0.774, and YES recall 0.386. On validation it achieved accuracy 0.738 and balanced accuracy 0.670. This result shows moderate skill with useful signal. It serves as a seasonal prior augmentation test. These notes emphasize leak-safe comparison across batches and clarify precision/recall tradeoffs for future revisions and ablations.
Clarified interpretation: The input features and model family are exactly as stated above; no additional signals are introduced. The model makes a single binary decision at the cutoff: has the daily maximum (Tmax) already occurred? YES precision reflects avoiding false early-peak calls, while YES recall reflects avoiding missed late-heating cases. Use these metrics strictly for relative ranking within the same split and thresholds, not as a trading PnL claim. If this variant underperforms, it usually means the added hypothesis is noisy, weak, or needs better calibration. Compare it to the closest baseline in this batch and focus on balanced accuracy and the precision/recall tradeoff together.

EXP16_ADD_ANALOG_KNN_PRIOR
EXP16_ADD_ANALOG_KNN_PRIOR is a LightGBM classifier experiment that focuses on kNN analog prior features. It uses minute plus MOS. The purpose is blend historical analog outcomes. On the test split it achieved accuracy 0.733, balanced accuracy 0.666, YES precision 0.777, and YES recall 0.402. On validation it achieved accuracy 0.745 and balanced accuracy 0.683. This result shows moderate skill with useful signal. It serves as a analog similarity benchmark. These notes emphasize leak-safe comparison across batches and clarify precision/recall tradeoffs for future revisions and ablations.
Clarified interpretation: The input features and model family are exactly as stated above; no additional signals are introduced. The model makes a single binary decision at the cutoff: has the daily maximum (Tmax) already occurred? YES precision reflects avoiding false early-peak calls, while YES recall reflects avoiding missed late-heating cases. Use these metrics strictly for relative ranking within the same split and thresholds, not as a trading PnL claim. If this variant underperforms, it usually means the added hypothesis is noisy, weak, or needs better calibration. Compare it to the closest baseline in this batch and focus on balanced accuracy and the precision/recall tradeoff together.

EXP17_MONOTONIC_CONSTRAINTS
EXP17_MONOTONIC_CONSTRAINTS is a LightGBM with monotone constraints experiment that focuses on physics consistent monotonic relationships. It uses minute plus MOS. The purpose is improve generalization with constrained effects. On the test split it achieved accuracy 0.728, balanced accuracy 0.665, YES precision 0.745, and YES recall 0.415. On validation it achieved accuracy 0.746 and balanced accuracy 0.688. This result shows moderate skill with useful signal. It serves as a physics consistency regularization test. These notes emphasize leak-safe comparison across batches and clarify precision/recall tradeoffs for future revisions and ablations.
Clarified interpretation: The input features and model family are exactly as stated above; no additional signals are introduced. The model makes a single binary decision at the cutoff: has the daily maximum (Tmax) already occurred? YES precision reflects avoiding false early-peak calls, while YES recall reflects avoiding missed late-heating cases. Use these metrics strictly for relative ranking within the same split and thresholds, not as a trading PnL claim. If this variant underperforms, it usually means the added hypothesis is noisy, weak, or needs better calibration. Compare it to the closest baseline in this batch and focus on balanced accuracy and the precision/recall tradeoff together.

EXP18_SEASONAL_2MODEL
EXP18_SEASONAL_2MODEL is a two model seasonal split experiment that focuses on warm vs cool season dynamics. It uses minute plus MOS. The purpose is separate seasonal regimes explicitly. On the test split it achieved accuracy 0.716, balanced accuracy 0.639, YES precision 0.784, and YES recall 0.333. On validation it achieved accuracy 0.748 and balanced accuracy 0.672. This result shows modest skill and partial signal capture. It serves as a core fusion benchmark. These notes emphasize leak-safe comparison across batches and clarify precision/recall tradeoffs for future revisions and ablations.
Clarified interpretation: The input features and model family are exactly as stated above; no additional signals are introduced. The model makes a single binary decision at the cutoff: has the daily maximum (Tmax) already occurred? YES precision reflects avoiding false early-peak calls, while YES recall reflects avoiding missed late-heating cases. Use these metrics strictly for relative ranking within the same split and thresholds, not as a trading PnL claim. If this variant underperforms, it usually means the added hypothesis is noisy, weak, or needs better calibration. Compare it to the closest baseline in this batch and focus on balanced accuracy and the precision/recall tradeoff together.

EXP19_REVISION_V2_MINIMAL
EXP19_REVISION_V2_MINIMAL is a LightGBM classifier experiment that focuses on minimal MOS revision block. It uses minute plus MOS. The purpose is test compact revision signals. On the test split it achieved accuracy 0.723, balanced accuracy 0.684, YES precision 0.663, and YES recall 0.528. On validation it achieved accuracy 0.743 and balanced accuracy 0.714. This result shows moderate skill with useful signal. It serves as a forecast update sensitivity test. These notes emphasize leak-safe comparison across batches and clarify precision/recall tradeoffs for future revisions and ablations.
Clarified interpretation: The input features and model family are exactly as stated above; no additional signals are introduced. The model makes a single binary decision at the cutoff: has the daily maximum (Tmax) already occurred? YES precision reflects avoiding false early-peak calls, while YES recall reflects avoiding missed late-heating cases. Use these metrics strictly for relative ranking within the same split and thresholds, not as a trading PnL claim. If this variant underperforms, it usually means the added hypothesis is noisy, weak, or needs better calibration. Compare it to the closest baseline in this batch and focus on balanced accuracy and the precision/recall tradeoff together.

EXP20_PROPER_OOF_STACK
EXP20_PROPER_OOF_STACK is a out of fold stacking experiment that focuses on leakage safe ensemble stacking. It uses model outputs. The purpose is test ensemble gains without leakage. On the test split it achieved accuracy 0.710, balanced accuracy 0.639, YES precision 0.731, and YES recall 0.357. On validation it achieved accuracy 0.720 and balanced accuracy 0.650. This result shows modest skill and partial signal capture. It serves as a ensemble benchmark to test combination gains. These notes emphasize leak-safe comparison across batches and clarify precision/recall tradeoffs for future revisions and ablations.
Clarified interpretation: The input features and model family are exactly as stated above; no additional signals are introduced. The model makes a single binary decision at the cutoff: has the daily maximum (Tmax) already occurred? YES precision reflects avoiding false early-peak calls, while YES recall reflects avoiding missed late-heating cases. Use these metrics strictly for relative ranking within the same split and thresholds, not as a trading PnL claim. If this variant underperforms, it usually means the added hypothesis is noisy, weak, or needs better calibration. Compare it to the closest baseline in this batch and focus on balanced accuracy and the precision/recall tradeoff together.
