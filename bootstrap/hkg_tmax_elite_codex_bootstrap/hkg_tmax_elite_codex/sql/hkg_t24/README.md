# HKG T24 SQL Assets

SQL assets for the HKG T+24 / H24N full strategy implementation belong here when they are not database migrations.

Use:

```text
migrations/postgres/
```

for durable schema migrations.

Use this folder for reviewed SQL views, report queries, and extraction query templates that the `code/src/hkg_t24/` package owns.
