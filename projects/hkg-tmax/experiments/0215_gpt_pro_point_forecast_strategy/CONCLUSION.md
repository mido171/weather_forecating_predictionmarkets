# Conclusion

The selected strategy is `B3_grouped_residual_shrinkage` at cutoff `23:59`. It scored MAE `0.92161` C and RMSE `1.18268` C on official-row yearly walk-forward validation. The raw official baseline at the same cutoff scored MAE `0.92749` C and RMSE `1.19152` C.
