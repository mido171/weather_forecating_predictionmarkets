# Legacy boundary

Everything below `legacy/` is historical and non-authoritative. Read it only
when current code/docs explicitly require provenance. Do not add new features,
run legacy network commands, or import legacy modules into active components.
Any code revived from here must move to its canonical owner with tests,
fail-closed defaults, and a migration note.
