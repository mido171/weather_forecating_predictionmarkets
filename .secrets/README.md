# Local secrets

This directory is ignored except for this guidance and explicit `*.example` placeholders.
Never place a real credential in a tracked file. Prefer environment variables or an approved
operating-system/provider secret store. Do not print secret values during validation.
