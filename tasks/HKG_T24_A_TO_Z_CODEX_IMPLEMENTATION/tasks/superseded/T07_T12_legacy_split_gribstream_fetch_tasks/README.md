# Superseded T07-T12 GribStream Fetch Tasks

The folders here are preserved for traceability only.

They were superseded by:

```text
tasks/not-completed/T07_T12_tactical_h24n_gribstream_backfill/
```

Reason: the previous split task structure and broad runner encouraged general NWP warehousing behavior. The active plan is now one consolidated tactical H24N GribStream backfill task using exact `/runs` `timesList` requests, compact location policies, and wide tactical storage.
