# Artifact Contents

The ZIP contains:

- 1 root Codex constitution (`AGENTS.md`);
- 1 executable first-goal program (`FIRST_GOALS.md`);
- 1 machine-readable goal graph;
- 6 specialist Codex subagents;
- 6 reusable Codex skills;
- 39 source entries with point-in-time roles;
- a detailed acquisition plan;
- 20+ research, evaluation, leakage, production, and operational documents;
- a backlog of 170 falsifiable experiment candidates;
- a complete per-experiment directory template;
- Python tooling for immutable archives, timestamps, as-of checks, settlement buckets, source snapshots, experiment creation, milestones, metrics, and manifests;
- shell/PowerShell bootstrap and archival scripts;
- automated tests and CI;
- a standalone copy of the first goals outside the ZIP.

Use `MANIFEST.json` for exact file hashes after generation.
